# Aurum Worker Placeholder

This directory is reserved for the market data worker and trading cycle worker.

The foundation cycle only provides an executable placeholder. Future issues will add:

- Binance Spot Testnet market data ingestion.
- Strategy indicator calculation.
- Trading cycle locks through Redis.
- Decision log persistence for every cycle.
- Paper/Testnet/Mainnet mode controls.
