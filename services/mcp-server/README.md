# Aurum MCP Server Placeholder

This directory is reserved for the read-only MCP server.

The foundation cycle only provides an executable placeholder. Future issues will add:

- Bearer token authentication.
- Scoped read-only resources.
- Redis-backed rate limiting.
- Audit logs for agent access.
- Resources such as market summary, portfolio status, trade history, and decision logs.
